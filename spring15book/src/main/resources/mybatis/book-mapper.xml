<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    <mapper namespace="book">
    <!-- 등록 -->
    <insert id="add">
    	insert into book(book_id,book_title,book_author,book_publication_date,book_price,book_publisher,book_page_count,book_genre) values(book_seq.nextval,#{book_title},#{book_author},#{book_publication_date},#{book_price},#{book_publisher},#{book_page_count},#{book_genre})
    </insert>
    <!-- 목록 조회 -->
    <select id="list" resultType="bookDto">
    	select * from book
    </select>
    <!-- 
    상세 조회
     - 이 구문은 실행을 위해 int 하나만 필요하므로 사실상 이름이 의미가 없다
     -->
    <select id="find" resultType="bookDto">
    	select * from book where book_id = #{book_id}
    </select>
    <!-- 수정 -->
    <update id="edit">
    	update book set book_title=#{book_title},
    	 book_author=#{book_author},
    	 book_publication_date=#{book_publication_date},
    	 book_price=#{book_price},
    	 book_publisher=#{book_publisher},
    	 book_page_count=#{book_page_count},
    	 book_genre=#{book_genre}
    	 where book_id=#{book_id}
    </update>
    <!-- 삭제 -->
    <delete id="del">
    	delete book where book_id = #{book_id}
    </delete>
    </mapper>