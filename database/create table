
--문제 3: 통신사 요금제 정보
-- 통신사가 무조건 2자리라면 고정문자열 처리
-- (장점) 빠른 처리 속도
-- (단점) 글자 수가 다를 수 없음
DROP TABLE plan;
CREATE TABLE plan(
telecom char(2),
name varchar2(60),
month_price number,
data number,
call number,
sms number
);

-- 메뉴 정보 테이블
-- (참고) 테이블 자료형에는 논리가 없다
-- y/n 으로 처리하거나 1/0 으로 처리
CREATE TABLE menu(
name varchar2(60),
TYPE varchar2(9),
price NUMBER,
event char(1)
);
